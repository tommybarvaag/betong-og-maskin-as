# Bleeding-edge install: allow freshly-published preview builds (pnpm 11 defaults to a 1-day quarantine)
minimum-release-age=0
# next@preview + react 19.2 + sanity peers don't always declare exact ranges for a preview; don't hard-fail
strict-peer-dependencies=false
auto-install-peers=true
